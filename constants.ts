import { Question } from './types';

// NOTE: Questions are now loaded asynchronously from all_questions.json to improve performance and avoid bundle size issues.
// This export is kept for legacy reference if needed, but will be empty initially.
export const ALL_QUESTIONS: Question[] = [];
